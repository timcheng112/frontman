{
  "name": "frontman",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "generate:digest": "node --experimental-strip-types scripts/generate-digest.ts",
    "generate:digest:llm": "node --experimental-strip-types scripts/generate-digest-llm.ts"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^5.0.0"
  }
}
